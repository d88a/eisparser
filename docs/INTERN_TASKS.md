# Актуальные задачи стажеру

## Статус на сегодня (2026-02-24)
Закрыто:
- Stage 2 selection переведен на pending-статусы (`raw`, `ai_error`).
- Stage 1 ограничен переменной `STAGE1_MAX_PAGES` (default 10).
- В Stage 2 добавлен учет `processed_reg_numbers` и `failed_reg_numbers`.
- В `DatabaseService` прокинут `database_url` во все репозитории.
- Добавлен лог backend (`PostgreSQL`/`SQLite`).
- В AI-диагностике маскируются секреты (`Authorization`, proxy credentials).
- В `run_stage2` исправлен сценарий `overwrite=true`.

---

## Задача 1: финальная проверка Stage 2 UX

### Цель
Подтвердить, что после ИИ-обработки пользователь понимает, куда исчезла закупка.

### Что проверить
1. На `Этап 2` выбрать 2-3 закупки и запустить обработку.
2. Убедиться, что UI показывает:
- сколько обработано;
- список `processed_reg_numbers`;
- список `failed_reg_numbers` (если есть);
- ссылку перехода на Stage 3.
3. Если карточка ушла из pending-списка, справа должен быть понятный текст о переносе на Stage 3.

### Артефакты
- 2 скриншота (до/после run_stage2);
- краткий лог проверки.

---

## Задача 2: проверка деплоя на NetAngels без поломки entrypoint

### Цель
Подтвердить безопасный деплой только кодовых файлов.

### Критично
Не менять:
- `/home/c77461/priv-mag.ru/app/asgi.py`
- `/home/c77461/priv-mag.ru/app/src/.env`

### Smoke-check (обязательно)
```bash
curl -i --max-time 10 -H "Host: priv-mag.ru" "http://$APP_IP:$APP_PORT/api/admin/zakupki_all?offset=0&limit=1"
PYTHONPATH=/home/c77461/priv-mag.ru/app/src /home/c77461/priv-mag.ru/.env/bin/python - <<'PY'
from services.database_service import DatabaseService
db = DatabaseService()
print("repo_is_postgres =", db.zakupki.is_postgres)
print("count =", len(db.zakupki.get_all()))
PY
```

### Ожидаемо
- API: `401` или `200`, но не `404`;
- `repo_is_postgres = True`.

---

## Задача 3: контроль cron-фона

### Цель
Подтвердить, что worker реально запускается по расписанию.

### Проверка
```bash
crontab -l
tail -n 200 /home/c77461/priv-mag.ru/app/src/results/logs/cron_worker.log
```

### Ожидаемо
- есть периодические строки `Cron iteration start: ...`;
- цикл завершается `Worker stopped` после `--max-cycles 1` (это норма в cron-режиме).

---

## Формат отчета стажера
1. Список проверенных пунктов.
2. Команды + фактический вывод.
3. Скриншоты UI Stage 2.
4. Что осталось нерешенным (если есть).

---

## Срочный блок: Stage 2 "висит"/долго стартует (выполняет стажер)

### Контекст
- На проде Stage 2 показывает большую очередь (`pending ~600+`), но фактическая обработка начинается с большой задержкой.
- Пользователь вручную SQL-индексы не создает; это зона ответственности стажера.

### Что сделать (обязательно)
1. Добавить индексы на прод-БД (через Adminer SQL-запросом):
```sql
CREATE INDEX IF NOT EXISTS idx_zakupki_status ON zakupki(status);
CREATE INDEX IF NOT EXISTS idx_ai_results_reg_number ON ai_results(reg_number);
CREATE INDEX IF NOT EXISTS idx_zakupki_processed_at ON zakupki(processed_at);
```

2. Оптимизировать worker Stage 2, чтобы не грузить весь pending дважды:
- В `src/services/worker_service.py` убрать тяжелую предварительную проверку через полный `get_stage2_pending_items()`.
- Проверять наличие задач легким способом: `get_stage2_pending_page(offset=0, limit=1)`.

3. Оптимизировать `run_stage2`:
- В `src/pipeline.py`, когда `reg_numbers` не переданы, брать не полный pending, а страницу сразу по `limit`.
- Не делать полный scan 600+ записей, если цикл обрабатывает только `--limit=10`.

### Проверки
1. До/после по времени старта Stage 2 в логе:
- От строки `Stage 2 started` до первой строки `[1/N] Обработка AI`.

2. Проверка, что очередь обрабатывается:
- есть `Cerebras status: 200`;
- `processed > 0` в итогах Stage 2 (для валидных записей).

3. Проверка, что UI не "зависает" на запуске:
- кнопка запуска отрабатывает;
- есть итоговый статус по обработанным/ошибкам.

### Definition of Done
- Stage 2 начинает обработку без длительной паузы.
- Нет лишних full-scan pending-очереди на каждом цикле.
- Производительность на очереди 600+ записей приемлемая при `limit=10`.

---

## UX-доработка Этапа 2: вкладка обработанных ИИ

### Задание
Исправить вкладку `Этап 2`:
1. Полностью убрать английские подписи в интерфейсе.
2. Кнопку `Pending` переименовать в `На проверке`.
3. Кнопку `Только что обработанные` заменить на `Все обработанные ИИ`.
4. Во вкладке `Все обработанные ИИ` показывать все обработанные закупки, а не только за последний час.
5. В списке `Все обработанные ИИ` сортировка должна быть: сверху самые последние обработанные (по `processed_at` по убыванию).

### Что изменить в коде
1. `src/web/templates/stage2.html`:
- заменить текст кнопок на русский:
- `На проверке`
- `Все обработанные ИИ`

2. `src/web/static/js/stage2.js`:
- убрать привязку к режиму «только что»;
- режим назвать, например, `processed`;
- после обработки автоматически переключать на `Все обработанные ИИ`.

3. `src/api/routes.py` (`/api/stage2`):
- для режима обработанных убрать фильтр по времени;
- отдавать все записи со статусом `ai_ready`;
- сортировать по `processed_at DESC`;
- оставить пагинацию.

### Критерии приемки
1. В `Этапе 2` нет английских слов.
2. Вкладка `Все обработанные ИИ` показывает все `ai_ready`.
3. Первыми в списке идут последние обработанные.
4. После запуска ИИ закупка не «пропадает», а видна во вкладке `Все обработанные ИИ`.

---

## UX-доработка Этапа 4: вкладка обработанных

### Задание
Сделать на `Этапе 4` отдельную вкладку `Обработанные` по аналогии с `Этапом 3`, чтобы можно было смотреть уже собранные закупки отдельно от очереди на сбор.

### Что должно быть в UI
1. На странице `Этап 4` две вкладки:
- `На сбор`
- `Обработанные`
2. Во вкладке `Обработанные` показывать закупки, по которым сбор объявлений уже выполнен.
3. Сортировка во вкладке `Обработанные`:
- сверху последние обработанные (по времени обработки по убыванию).
4. После успешного сбора на `Этапе 4` закупка должна переходить из `На сбор` в `Обработанные`, а не «исчезать» для пользователя.
5. Верхняя шапка управления (чекбокс, поле количества объявлений, кнопка `Собрать объявления`) должна быть статичной (sticky) и не прокручиваться вместе со списком закупок.

### Что изменить в коде
1. `src/web/templates/stage4.html`:
- добавить переключатель вкладок `На сбор` / `Обработанные`.
- выделить блок шапки управления в отдельный контейнер для закрепления.

2. `src/web/static/js/stage4.js`:
- добавить режим фильтра для обработанных;
- при успешном сборе автоматически обновлять список и показывать запись во вкладке `Обработанные`;
- сохранить пагинацию/подгрузку.

3. `src/web/static/css/style.css` (или соответствующий css для Stage 4):
- сделать шапку Stage 4 закрепленной (`position: sticky`, `top: 0`, `z-index` выше списка, непрозрачный фон, нижняя граница/тень);
- проверить на desktop и mobile, чтобы шапка не перекрывала контент и не «прыгала» при скролле.

4. `src/api/routes.py` (`/api/stage4` или соответствующий endpoint):
- добавить режим выдачи `processed`;
- возвращать закупки со статусами, означающими завершенный Stage 4 (актуальные статусы взять из текущей бизнес-логики проекта);
- сортировать по времени обработки Stage 4 по убыванию.

### Критерии приемки
1. На `Этапе 4` есть две рабочие вкладки: `На сбор` и `Обработанные`.
2. Во вкладке `Обработанные` отображаются все обработанные на Stage 4 закупки.
3. Последние обработанные показываются первыми.
4. После запуска сбора закупка видна во вкладке `Обработанные` и не теряется для пользователя.
5. Шапка с кнопкой `Собрать объявления` остается на месте при прокрутке списка закупок.

---

## Блок по аудиту: приоритеты перед переносом на VDS

### P0: безопасность и предсказуемый запуск
1. Убрать legacy Gemini-код из `src/services/ai_service.py`:
- удалить `_get_client`;
- удалить неиспользуемые gemini-импорты и переменные;
- оставить только repo-методы, используемые в `pipeline.py`.

2. Подготовить единый запуск без хаков импорта:
- проверить entrypoint для ASGI и CLI;
- зафиксировать корректный `PYTHONPATH`/module-run для VDS;
- исключить случайные зависимости от локального окружения.

3. Проверить admin-аутентификацию:
- не хранить пароль в cookie в открытом виде;
- зафиксировать текущий риск в `docs/PROJECT.md`;
- предложить безопасную схему (подписанный токен, TTL).

### P1: надежность пайплайна 1-4
1. Реализовать режим "новая закупка -> цепочка этапов":
- при появлении новой закупки Stage 1/2 запускаются сразу;
- после `ai_ready` Stage 3/4 запускаются без ручного поиска;
- ввести четкий retry-контур для `ai_error` и `stage4_error`.

2. Убрать "разнобой" обработки:
- разделить фон на два воркера:
- `worker-ingest` (Stage 1-2),
- `worker-listing` (Stage 3-4);
- подтвердить, что один тяжелый этап не блокирует остальные.

3. Проверить и унифицировать статусы:
- `raw -> ai_ready/ai_error -> url_ready -> stage4_done/stage4_error`;
- задокументировать источники переходов в коде (кто и где меняет статус).

### P1: наблюдаемость и диагностика
1. Добавить явный лог прогресса по каждой закупке:
- `reg_number`, текущий этап, итог (`ok/error/skip`), причина;
- единый формат причин ошибок для Stage 2 и Stage 4.

2. Подготовить быстрые диагностические команды для VDS:
- проверка очередей по статусам;
- проверка зависших записей (долго в одном статусе);
- проверка длительности этапов.

### Критерии приемки по аудиту
1. В коде нет активного Gemini-контура.
2. Новые закупки идут по цепочке этапов предсказуемо.
3. Retry-очереди не теряются и видны в UI.
4. Два воркера работают независимо и не блокируют друг друга.
5. Документация для VDS запуска полностью воспроизводима.
