# Задания стажеру (спринт 2026-03-02)

Источник постановки:
1. `docs/ARCHITECT_2_ACTION_REPORT_2026-03-02.md`
2. `docs/ARCHITECT_2_PLAYBOOK.md`

Контекст:
1. Базовые проверки зелёные (`compileall`, `pytest`).
2. Предыдущие замечания по access/retry/docs закрыты.
3. На текущий спринт у нас 3 целевые задачи: `A`, `B`, `C`.

Статус выполнения (обновлено 2026-03-02):
1. `A` закрыт: в `pipeline.py` и `worker_service.py` статус-коды переведены на `ZakupkaStatus`/наборы из `src/models/statuses.py`.
2. `B` закрыт: добавлен тест деградации ingest-цикла `tests/test_worker_ingest_resilience.py`.
3. `C` закрыт: `tests/test_overrides_smoke.py` проверяет сохранение override через `DatabaseService`/репозиторий без hardcoded SQLite-пути.

## 0) Общие правила

### Цель
Закрыть технический долг перед следующим релизом без изменения продуктового поведения.

### Архитектурные инварианты (не нарушать)
1. Stage 2 pending: только `raw`, `ai_error`.
2. Stage 4 queue: только `url_ready`, `stage4_error`.
3. Lifecycle: `raw -> ai_ready | ai_error -> url_ready -> stage4_done | stage4_error`.
4. Канонический источник статусов: `src/models/statuses.py`.

### Что запрещено
1. Не ослаблять admin-контур.
2. Не менять API-контракты без отдельного согласования.
3. Не коммитить секреты и `.env`.
4. Не вносить правки на прод-сервере вручную в обход PR.

---

## 1) Задача A (P1): убрать магические строки статусов

### Цель
Убрать строковые статусы из бизнес-логики и заменить на константы из `src/models/statuses.py`.

### Приоритет
`P1` (обязательно в этом спринте).

### Разрешенные файлы
1. `src/pipeline.py`
2. `src/services/worker_service.py`
3. `tests/*` (если нужны правки тестов)
4. `docs/PROJECT.md` (только если потребуется уточнение контрактов)

### Шаги выполнения
1. Импортировать и использовать:
   1. `ZakupkaStatus`
   2. `STAGE2_PENDING_STATUSES`
   3. `STAGE4_QUEUE_STATUSES`
2. В `src/pipeline.py` заменить сравнения и `update_status(...)` со строк:
   1. `"raw"`, `"ai_error"`, `"ai_ready"`, `"url_ready"`, `"stage4_done"`, `"stage4_error"`
   2. на соответствующие константы.
3. В `src/services/worker_service.py` заменить:
   1. ручные списки статусов для Stage 3/4 выборок,
   2. строковый `update_status(..., "stage4_error")`,
   3. на константы/наборы из `statuses.py`.
4. Оставить строковые статусы только в текстах логов/сообщений, но не в условиях и переходах.
5. Добавить/обновить тесты, если после рефакторинга меняется покрытие.

### Локальная самопроверка (обязательно приложить в отчет)
1. `python -m compileall -q src`
2. `python -m pytest -q`
3. `rg -n "['\\\"](raw|ai_ready|ai_error|url_ready|stage4_done|stage4_error)['\\\"]" src/pipeline.py src/services/worker_service.py`

### Критерий приемки
1. В `pipeline.py` и `worker_service.py` нет строковых статус-кодов в бизнес-логике.
2. Все переходы/проверки статусов идут через константы.

---

## 2) Задача B (P1): тест деградации ingest-цикла

### Цель
Подтвердить тестом, что ingest-цикл корректно деградирует при временной недоступности ЕИС и не падает в crash-loop.

### Приоритет
`P1` (обязательно в этом спринте).

### Разрешенные файлы
1. `tests/test_worker_ingest_resilience.py` (новый файл)
2. `src/services/worker_service.py` (только если для тестируемости нужен минимальный рефакторинг)
3. `src/pipeline.py` (минимальные правки для тестируемости, если потребуется)
4. `docs/WORKER_VDS.md` (если добавляешь отражение сценария в runbook)

### Шаги выполнения
1. Сделать тест уровня `WorkerService/Pipeline`:
   1. Stage 1 возвращает временную ошибку/пустой результат.
   2. `run_cycle_ingest(...)` не бросает исключение наружу.
   3. Цикл завершает итерацию корректно.
2. Проверить, что в логе цикла есть диагностируемая причина (`reason`/ошибка Stage 1).
3. Зафиксировать тестовые двойники (fake pipeline/services), чтобы тест был детерминированным и быстрым.
4. При необходимости слегка упростить инъекцию зависимостей в воркер, но без изменения бизнес-смысла этапов.

### Локальная самопроверка
1. `python -m compileall -q src`
2. `python -m pytest -q tests/test_worker_ingest_resilience.py`
3. `python -m pytest -q`

### Критерий приемки
1. Новый тест стабильно проходит.
2. Поведение соответствует runbook: корректная деградация, без crash-loop.

---

## 3) Задача C (P2): переносимый overrides-smoke

### Цель
Убрать жесткую привязку smoke-теста к локальному SQLite пути и сделать его переносимым для SQLite/PostgreSQL.

### Приоритет
`P2` (после задач A и B).

### Разрешенные файлы
1. `tests/test_overrides_smoke.py`
2. `tests/conftest.py` (если нужно вынести конфиг)
3. `docs/DEPLOY_AND_RUN.md` (опционально, если меняется способ запуска smoke)

### Шаги выполнения
1. Убрать hardcode:
   1. `DB_PATH = "D:/Anna/eisparser/results/eis_data.db"`
2. Ввести определение backend в тесте:
   1. если есть `DATABASE_URL` -> проверка через репозиторий/сервис (без прямого sqlite-файла),
   2. если `DATABASE_URL` пуст -> SQLite через параметризуемый путь.
3. Для неподходящего окружения использовать корректный `pytest.skip(...)` с понятной причиной.
4. Сохранить текущий смысл smoke: override записался и читается обратно через API.

### Локальная самопроверка
1. `python -m compileall -q src`
2. `python -m pytest -q tests/test_overrides_smoke.py`
3. `python -m pytest -q`

### Критерий приемки
1. Тест работает или корректно `skip` в SQLite и PostgreSQL режимах.
2. Нет привязки к локальному пути разработчика.

---

## 4) Порядок выполнения
1. Сначала `A` (магические строки).
2. Затем `B` (деградация ingest).
3. Затем `C` (переносимость smoke).

Без закрытия `A + B` задача спринта не считается принятой.

---

## 5) Формат отчета стажера (обязательный)
1. Список измененных файлов по каждой задаче `A/B/C`.
2. Что именно исправлено по пунктам (кратко и предметно).
3. Фактический вывод команд:
   1. `python -m compileall -q src`
   2. `python -m pytest -q`
   3. доп. точечные тесты по новым файлам.
4. Короткий smoke:
   1. ingest воркер (1 цикл),
   2. 1-2 API запроса для sanity check.
5. Остаточные риски и что предлагается на следующий спринт.

Отчет без команд и фактического вывода не принимается.

---

## 6) Чеклист приемки архитектором
1. Нет магических статус-строк в бизнес-логике `pipeline.py` и `worker_service.py`.
2. Есть отдельный тест деградации ingest-цикла.
3. `test_overrides_smoke.py` переносим и не зависит от локального `D:/...`.
4. `python -m compileall -q src` проходит.
5. `python -m pytest -q` проходит.
