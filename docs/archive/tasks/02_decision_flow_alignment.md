# Задача 2. Согласовать решения Stage 1 -> Stage 2

## Цель
Избавиться от разрыва между `selected` и `approved`.

## Вариант A (рекомендуемый)
- Использовать только `selected` для запуска Stage 2.
- В `run_stage2` получать `selected` решения из `decisions`.

## Вариант B
- В `add_to_stage2` записывать `approved`.
- Оставить `run_stage2` как есть.

## Что выбрать
Выбрать один вариант и применить последовательно.

## Проверка
- Закупка, добавленная через Stage 1, действительно обрабатывается Stage 2.

## Файлы
- `src/api/routes.py`
- `src/services/view_service.py` (если нужно)
