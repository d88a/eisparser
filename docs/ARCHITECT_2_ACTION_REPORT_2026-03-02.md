# Отчет для Архитектора-2: что исправить и что делать далее

Дата: 2026-03-02  
Проверяющий: Главный архитектор  
Контур: `EIS Parser v2.0`, VDS `213.189.219.55`

## 1) Результат проверки

Базовый статус хороший:
1. `compileall` проходит.
2. `pytest` проходит (`6 passed, 1 skipped`).
3. Основные P0/P1/P2 задачи по документам и тестам закрыты.

Есть технический долг, который нужно закрыть до следующего релиза.

## 2) Что исправить (обязательно)

## A. Убрать магические строки статусов из бизнес-логики

Проблема:
1. В коде остаются строковые статусы (`"raw"`, `"ai_ready"`, `"url_ready"`, `"stage4_error"` и т.д.) вне `src/models/statuses.py`.
2. Это риск расхождений между слоями и скрытых регрессий при рефакторинге.

Где исправлять в первую очередь:
1. `src/pipeline.py`
2. `src/services/worker_service.py`

Что сделать:
1. Использовать только константы `ZakupkaStatus`, `STAGE2_PENDING_STATUSES`, `STAGE4_QUEUE_STATUSES`.
2. Убрать ручные строковые списки в выборках и update_status.

Критерий приемки:
1. Нет строковых статус-кодов в `pipeline.py` и `worker_service.py` (кроме логов/комментариев).
2. Все проверки и обновления статусов идут через константы.

---

## B. Усилить проверку устойчивости ingest-цикла

Проблема:
1. Есть unit-тесты retry в downloader, но нет отдельной проверки поведения ingest-цикла при временной недоступности ЕИС.
2. Риск: реальный воркер может деградировать не так, как ожидаем.

Что сделать:
1. Добавить тест уровня `WorkerService`/`Pipeline` для сценария:
   1. Stage 1 временно недоступен.
   2. Цикл воркера не падает и корректно завершает итерацию.
   3. В логе есть диагностируемый reason.

Критерий приемки:
1. Новый тест проходит стабильно.
2. Поведение соответствует runbook: “не crash-loop, корректная деградация”.

---

## C. Привести smoke override к переносимому виду

Проблема:
1. `tests/test_overrides_smoke.py` завязан на локальный SQLite путь `D:/Anna/eisparser/results/eis_data.db`.
2. На VDS и PostgreSQL это неуниверсально.

Что сделать:
1. Вынести путь/бекенд в конфиг теста.
2. Для SQLite использовать параметризированный путь.
3. Для PostgreSQL читать из `DATABASE_URL` и проверять через репозиторий/сервис, а не жесткий путь к файлу.

Критерий приемки:
1. Тест корректно работает или корректно `skip` в обоих режимах (SQLite/PostgreSQL).

## 3) Что делать далее (план на ближайшие шаги)

## Шаг 1 (сегодня): техническая консолидация
1. Закрыть пункты A и B.
2. Запустить:
   1. `python -m compileall -q src`
   2. `python -m pytest -q`
3. Обновить `docs/INTERN_TASKS.md` по факту закрытия.

## Шаг 2 (после консолидации): стабилизация Stage 1 в проде
1. Проверить ingest-логи на регулярность новых загрузок после регламентов ЕИС.
2. Подтвердить, что `PROXY_URL`/retry параметры заданы и документированы.
3. Зафиксировать короткий операционный чек-лист в `docs/WORKER_VDS.md`.

## Шаг 3 (продукт): подготовка бесплатной пользовательской версии
1. Оставить `USER_ACCESS_MODE=PUBLIC` как baseline.
2. Не смешивать пользовательский контур с админским.
3. Внедрять только безопасные заготовки под будущие тарифы (без включения ограничений).

## 4) Формат отчета от Архитектора-2 после исправлений

Обязательные пункты:
1. Список измененных файлов.
2. Что исправлено по пунктам A/B/C.
3. Вывод команд:
   1. `python -m compileall -q src`
   2. `python -m pytest -q`
4. Короткий smoke по воркерам и API.
5. Остаточные риски и предложения на следующий спринт.

## 5) Приоритеты

1. P1: A (магические строки статусов)  
2. P1: B (тест деградации ingest-цикла)  
3. P2: C (переносимость override-smoke)  

До закрытия A+B релиз “как есть” не блокируется критически, но остается риск регрессий при следующем изменении status-flow.
